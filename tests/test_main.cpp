#include <QTest>

// Include test classes here
// #include "test_star.h"
// #include "test_project.h"

int main(int argc, char *argv[])
{
    QCoreApplication app(argc, argv);

    int status = 0;

    // Run tests
    // status |= QTest::qExec(new TestStar, argc, argv);
    // status |= QTest::qExec(new TestProject, argc, argv);

    return status;
}